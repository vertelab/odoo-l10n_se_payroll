<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sequence for late.check.in -->
    <record id="late_check_in_sequence" model="ir.sequence">
        <field name="name">Late Check-in</field>
        <field name="code">late.check.in</field>
        <field name="prefix">LC/</field>
        <field name="padding">5</field>
        <field name="number_next">1</field>
        <field name="number_increment">1</field>
    </record>

    <!-- Tree View -->
    <record id="late_check_in_view_tree" model="ir.ui.view">
        <field name="name">late.check.in.tree</field>
        <field name="model">late.check.in</field>
        <field name="arch" type="xml">
            <tree string="Late Check-ins" editable="bottom">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="date"/>
                <field name="late_minutes"/>
                <field name="penalty_amount"/>
                <field name="state" widget="badge"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="late_check_in_view_form" model="ir.ui.view">
        <field name="name">late.check.in.form</field>
        <field name="model">late.check.in</field>
        <field name="arch" type="xml">
            <form string="Late Check-in">
                <header>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,approved"/>
                    <button name="action_approve" string="Approve"
                            type="object" class="btn-primary"
                            attrs="{'invisible': [('state', '!=', 'draft')]}"
                            groups="hr.group_hr_manager"/>
                    <button name="action_refuse" string="Refuse"
                            type="object" class="btn-secondary"
                            attrs="{'invisible': [('state', '!=', 'draft')]}"
                            groups="hr.group_hr_manager"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"
                                   options="{'no_quick_create': True, 'no_create_edit': True}"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="late_minutes"/>
                            <field name="penalty_amount"/>
                        </group>
                    </group>
                    <group string="Attendance Reference">
                        <field name="attendance_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="late_check_in_view_search" model="ir.ui.view">
        <field name="name">late.check.in.search</field>
        <field name="model">late.check.in</field>
        <field name="arch" type="xml">
            <search string="Search Late Check-ins">
                <field name="employee_id"/>
                <field name="date"/>
                <filter string="Draft" name="state_draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter string="Approved" name="state_approved"
                        domain="[('state', '=', 'approved')]"/>
                <group expand="0" string="Group By">
                    <filter string="Employee" name="group_employee"
                            domain="[]" context="{'group_by': 'employee_id'}"/>
                    <filter string="Date" name="group_date"
                            domain="[]" context="{'group_by': 'date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="late_check_in_action" model="ir.actions.act_window">
        <field name="name">Late Check-ins</field>
        <field name="res_model">late.check.in</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_group_employee': 1}</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_late_check_in"
              name="Late Check-ins"
              parent="hr_attendance.menu_hr_attendance_root"
              action="late_check_in_action"
              sequence="21"
              groups="hr.group_hr_user"/>
</odoo>
